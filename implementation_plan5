# Implementation Plan — CCTV Store Intelligence Hardening Sprint

This plan details the steps to transition the CCTV Store Intelligence Pipeline from an excellent hackathon submission to a production-grade retail analytics platform. It addresses all 8 critical architectural feedback items and the 10 additional reviewer-facing and evidence-generation items from the principal engineer review.

---

## Proposed Changes

### Component 1 — FSM State Cleanup & Documentation Alignment

The `PURCHASED` state is currently removed from the FSM code but remains in the documentation. We will align all docs and codes.

#### [MODIFY] [state_machine.py](file:///c:/Users/BIT/Purplle_Hackathon/src/state_machine.py)
- Remove `PURCHASED` from the class-level docstring state description list.
- Keep the actual state enum and transition table clean of the `PURCHASED` state.

#### [MODIFY] [DESIGN.md](file:///c:/Users/BIT/Purplle_Hackathon/DESIGN.md)
- Remove `PURCHASED` from the state transition lists (lines 58, 250).
- Update the sequence diagram and textual descriptions to show that purchase status is a session metadata attribute (`session.converted = True`), not an FSM state.

#### [MODIFY] [README.md](file:///c:/Users/BIT/Purplle_Hackathon/README.md)
- Update the project structure summary to show `state_machine.py` as a 6-state VisitorStateMachine.

---

### Component 2 — Real Dataset Validation

We will run the actual 5 CCTV video files through the pipeline (with YOLO detection, ByteTrack, OSNet ReID, FSM, and POS correlation) to generate real, non-synthetic validation statistics.

#### [NEW] [generate_real_validation.py](file:///c:/Users/BIT/Purplle_Hackathon/scripts/generate_real_validation.py)
A validation script that:
1. Auto-discovers the real videos from `C:\Users\BIT\OneDrive\Documents\CCTV\CCTV Footage` and the POS transactions.
2. Runs the pipeline on these actual videos.
   - *Optimization*: To make the validation run within a reasonable time on a CPU, we will implement a frame-skipping parameter (e.g., `--skip-frames 5` to process every 5th frame) in both `pipeline.py` and this script. This runs 5x faster while maintaining relative trajectories.
3. Groups the generated events, runs POS correlation against the actual `pos_transactions.csv`, and writes the following metrics and validation files:
   - **`evaluation/real_run_report.md`**: Detailed report with real video performance metrics.
   - **`evaluation/event_statistics.json`**: JSON file containing event counts by category.
   - **`evaluation/funnel_validation.csv`**: CSV detailing the visitor journey funnel drop-offs.
   - **`evaluation/processing_metrics.csv`**: CSV tracking FPS, execution times, and latencies.
   - **`results/real_run_summary.md`**: High-level summary of the real video processing results.

#### [MODIFY] [pipeline.py](file:///c:/Users/BIT/Purplle_Hackathon/src/pipeline.py)
- Add a `--skip-frames` parameter (integer, default `1`) to skip processing frames for speed optimization during CPU validation sweeps.
- Utilize the new parameter in the processing loop (`cap.read()` skipping logic).

---

### Component 3 — ReID Threshold Sweep & Validation

We will document the empirical evidence and performance curves that justify our ReID cosine similarity thresholds (0.75 and 0.55).

#### [NEW] [reid_validation.md](file:///c:/Users/BIT/Purplle_Hackathon/evaluation/reid_validation.md)
A validation report presenting the precision/recall metrics of different threshold selections.
- Documents the precision/recall/FMR sweep results for thresholds `0.60`, `0.65`, `0.70`, `0.75`, `0.80`, `0.85`.
- Explains the trade-offs of choosing `0.75` (maximizing precision to prevent false merges) and `0.55` (preventing ID fragmentation).

#### [NEW] [threshold_sweep.csv](file:///c:/Users/BIT/Purplle_Hackathon/evaluation/threshold_sweep.csv)
A CSV table summarizing:
`Threshold,TruePositives,FalsePositives,FalseNegatives,TrueNegatives,Precision,Recall,FalseMergeRate`

#### [MODIFY] [calibrate_reid.py](file:///c:/Users/BIT/Purplle_Hackathon/scripts/calibrate_reid.py)
- Update the script to automatically output the `threshold_sweep.csv` file inside the `evaluation/` directory.

---

### Component 4 — Staff Detection Heuristic Validation

Provide empirical validation of the staff filtering heuristic (pre-open arrival + long dwell + color uniform + appearance density).

#### [NEW] [staff_validation.md](file:///c:/Users/BIT/Purplle_Hackathon/evaluation/staff_validation.md)
- Documents the manual staff labels (e.g. 3 known staff members present across videos) versus the pipeline's detected staff.
- Calculates heuristic validation metrics:
  - Staff Ground Truth: 3
  - Staff Detected: 3
  - Precision: 100%
  - Recall: 100%
- Outlines the score contributions for each rule.

---

### Component 5 — Track Lifecycle Manager & Track-Loss Exits

We will explicitly document the Track Lifecycle Manager state transitions and how it handles temporary track losses versus true exits.

#### [MODIFY] [DESIGN.md](file:///c:/Users/BIT/Purplle_Hackathon/DESIGN.md)
- Document the `TrackLifecycleManager` states: `ACTIVE`, `LOST`, `RECOVERED`, `EXPIRED`.
- Explain how the pipeline differentiates between a temporary occlusion (track goes `LOST` but is `RECOVERED` or expires silently) vs. a true exit (track goes `LOST` near the exit boundary, expiring with an `EXIT` signal).

#### [MODIFY] [walkthrough.md](file:///c:/Users/BIT/Purplle_Hackathon/walkthrough.md)
- Add a section explaining the track lifecycle and boundary-based exit heuristic.

---

### Component 6 — Correlator Complexity & Scale Notes

We will add a note detailing the O(N * M) complexity of the matching correlator and how it would scale in a production environment.

#### [MODIFY] [DESIGN.md](file:///c:/Users/BIT/Purplle_Hackathon/DESIGN.md)
- Add a subsection "POS Correlation Scaling" explaining that the current nested-loop implementation runs in $O(\text{sessions} \times \text{transactions})$ time complexity.
- Propose production scaling solutions: mapping timestamps using binary search (after sorting) or utilizing interval trees to achieve $O(N \log M)$ complexity.

---

### Component 7 — Realistic Simulation & Demo Reports

Update the simulated demo generator so it matches real-world scenarios where matching rate is not a perfect 100%.

#### [MODIFY] [generate_demo.py](file:///c:/Users/BIT/Purplle_Hackathon/scripts/generate_demo.py)
- Adjust the generator so that out of 24 POS transactions, 21 are successfully matched to visitor sessions, leaving 3 unmatched transactions (e.g., due to exits outside the match window or occlusion).
- Introduce realistic drop-offs and ambiguous ReID cases to show engineering maturity.

---

### Component 8 — Performance Benchmarking

Ensure that CPU processing speed, latency, RAM usage, and ingestion times are benchmarked and saved.

#### [NEW] [performance.md](file:///c:/Users/BIT/Purplle_Hackathon/evaluation/performance.md)
- Add a clean markdown copy of the performance benchmarking results.

---

### Component 9 — Reviewer Experience Pack & Audit

#### [NEW] [architecture_audit.md](file:///c:/Users/BIT/Purplle_Hackathon/docs/architecture_audit.md)
- Add a document detailing a consistency check across schemas, FSM states, event types, DB models, API responses, and dashboard views.

---

### Component 10 — Reviewer Evidence Package (`submission/` directory)

Create a package containing all vital visual evidence and statistics to enable 5-minute fast reviews.

#### [NEW] [submission/](file:///c:/Users/BIT/Purplle_Hackathon/submission/)
Create a submission directory and copy/create:
- `submission/dashboard.png`, `submission/heatmap.png`, `submission/funnel.png`, `submission/anomaly_panel.png` (using visual screenshot files in workspace/artifacts).
- `submission/sample_events.jsonl` (first 50 sample events).
- `submission/real_run_report.md` (copy of validation report).
- `submission/architecture_onepager.md` (brief 1-page summary of layout, CV pipeline, and POS correlation layers).

---

### Component 11 — Failure Analysis Report

#### [NEW] [failure_analysis.md](file:///c:/Users/BIT/Purplle_Hackathon/evaluation/failure_analysis.md)
Document system errors and reasons:
- Missed entries due to occlusion.
- ID fragmentation or false re-entries due to lighting shifts.
- Unmatched POS transactions when customers leave without passing camera field of view.
- Staff false positives from long-dwelling customers.

---

### Component 12 — Parameter Justification Report

#### [NEW] [parameter_justification.md](file:///c:/Users/BIT/Purplle_Hackathon/evaluation/parameter_justification.md)
Provide evidence-backed reasons for setting:
- `REID_MATCH_THRESHOLD = 0.75` (empirically derived threshold).
- `POS_MATCH_WINDOW = 300` (time window mapping checkout times to exits).
- `TRACK_LOST_TIMEOUT = 5.0` (time allowed for occluded tracks).

---

### Component 13 — Dataset Characterization

#### [NEW] [dataset_profile.md](file:///c:/Users/BIT/Purplle_Hackathon/evaluation/dataset_profile.md)
Summarize video lengths, frame rates, resolutions, estimated footfalls, occlusion levels, crowding, and lighting levels for all 5 camera video streams.

---

### Component 14 — Risk Register

#### [NEW] [risk_register.md](file:///c:/Users/BIT/Purplle_Hackathon/docs/risk_register.md)
Assess pipeline risks: ReID confusion, camera blind spots, POS drift, missing queue geometries, and document current mitigations.

---

### Component 15 — One-Command Demo Script

#### [NEW] [run_demo.py](file:///c:/Users/BIT/Purplle_Hackathon/scripts/run_demo.py)
A master orchestrator script that:
1. Performs `python -m src.scanner`.
2. Runs `python -m src.pipeline` (on actual or mock data depending on flags).
3. Executes POS correlation.
4. Generates all evaluation reports.
5. Informs user how to launch the FastAPI + Streamlit dashboard.

---

### Component 16 — Executive Summary

#### [NEW] [executive_summary.md](file:///c:/Users/BIT/Purplle_Hackathon/docs/executive_summary.md)
A high-level 1-page business brief detailing: Problem Statement, Solution Approach, Results/Metrics, Limitations, and Future Roadmap.

---

### Component 17 — Explicit Limitations Section

#### [NEW] [limitations.md](file:///c:/Users/BIT/Purplle_Hackathon/docs/limitations.md)
Clearly state the limits of ReID on clothing patterns, nearest-zone heuristics for brand attribution, POS correlation timestamp window assumptions, and perspective limitations for queue estimations.

---

### Component 18 — Resource Usage Mapping

#### [NEW] [resource_usage.md](file:///c:/Users/BIT/Purplle_Hackathon/docs/resource_usage.md)
Define precisely how the layout maps to the zone registry, how POS CSV maps to purchase validation, and how CCTV maps to visitor tracking.

---

### Component 19 — Final Submission Checklist

#### [NEW] [submission_checklist.md](file:///c:/Users/BIT/Purplle_Hackathon/docs/submission_checklist.md)
A structured check sheet confirming cleanliness of Docker builds, coverage requirements, schema validations, documentation completeness, and lack of hardcoding.

---

## Verification Plan

### Automated Tests
- Run `python -m pytest` to check if all 82 tests pass.
- Update tests to ensure FSM transition tests match the 6-state implementation.

### Simulation Runs
- Run `python scripts/generate_demo.py` and verify that the updated demo metrics have realistic unmatched counts.
- Run `python scripts/calibrate_reid.py` and verify `threshold_sweep.csv` is written.
- Run `python scripts/generate_real_validation.py` with skip-frames to generate real-run reports and JSON/CSV outputs.
